# Devlog


## Entry 1
Created sample `pygame-ce` initial window
based from example from the `pygame-ce` documentation pages.

Such documentation can be found by typing the following command into the Terminal:

`python3 -m pygame.docs`

## Entry 2
Created the `logs` directory in project root and wrote a log.
Logs are written as such: "devlog-M-D-Y.md"

These logs are markdown files. "M" means *month* created; "D" and "Y" mean *day* and *year* respectively.

## Entry 3
Created the Chicken Class, and implemented a search for MOUSEDOWN events,
that are converted by pybag into simple taps once this game is running
in the browser.

Wrote the `chicken-match-algorithms.txt` text file for programming simple 
algorithms before implementing them in code. Placed file in:
`/logs/chicken-match-algorithms.txt`

Decided to write the Color and Chicken position in a more grid style of 
lists within lists, with one list being the "coop" and the others being
the rows, and the contents of the list being the columns. (Y, and X)

My bluetooth earbuds ran out of battery; I am held subject to the sappy 
romance music of the car that I am riding in while writing this sentence.
Car-Poolers can't be choosers. :(

## Entry 4
I was about to commit the work I did under Entry 3 to the git repository, but then I remembered that calling the algorithms file: `chicken-match-algorithms.txt` was really unnecessary.

Renamed it to simply `algorithms.txt`
