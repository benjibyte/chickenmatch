# Devlog

## Entry 1
Moved `.gitignore` so it's in the root by `.git`
Researched why LazyVim wouldn't let me navigate my python virtual environment

## Entry 2
Found out that LazyVim won't let me navigate my python virtual environment through it's "snack" file explorer because of the `.gitignore` file. 

I don't really have anything to hide for this project, so I deleted that file. I can now use `<leader> E` to open the file explorer in `nvim` and navigate through the virtual environment directory.
